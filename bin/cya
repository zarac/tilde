#!/bin/env sh
# when done with a session

pushd ~/.task; git add -u; git commit -m "cya / $HOST"; git push origin master; popd
pushd ~/me/notes; git add .; git commit -m "cya / $HOST"; git push origin master; popd
